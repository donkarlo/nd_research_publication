\begin{frame}[allowframebreaks]{Control - General explanation}
    Instead of using GPS set points, give it the language with the goal to keep suprise poise as low as 0.1. No crash with the walls. Numbers will come later.
    
    
    Flight controller: PX4 (Software)
    
    FCU: Pixhawk4 (Hardware)
    
    
    \section{Goal (set-point)}
        A composite goal must be given to the planning that should include:
        
        Keep suprise poise as low as possible in controller cost function.
    
    
    \section{Cost function}
        
        The cost function in model predictive controller (MPC)
        \[
            J = \sum_{k=0}^{N-1} \left[ \left( x_k - x_k^{\mathrm{ref}} \right)^T Q \left( x_k - x_k^{\mathrm{ref}} \right) + u_k^T R u_k \right]
        \]
        
        In this research, X is once the Pose and another time with the anomally value
        
        \begin{figure}[H]
            \centering
            \includegraphics[width=0.9\textwidth]{control_with_pose_as_set_point/a_typical_controller.png}
        \end{figure}
        
        Both drones use \textbf{MPC} to reach their next set-point
        
        Feedback system from a feedback that is formed of resource\_gain and goal\_gain
\end{frame}