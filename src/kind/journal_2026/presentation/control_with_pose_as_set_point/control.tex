\begin{frame}{Planning and plan}
    Instead of using GPS set points, give it the language with the goal to keep suprise poise as low as 0.1. No crash with the walls. Numbers will come later.
    
    Flight controller: PX4
    
    FCU: Pixhawk
    
    \[
        J = \sum_{k=0}^{N-1} \left[ \left( x_k - x_k^{\mathrm{ref}} \right)^T Q \left( x_k - x_k^{\mathrm{ref}} \right) + u_k^T R u_k \right]
    \]
\end{frame}